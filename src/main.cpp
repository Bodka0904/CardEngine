//#include "Window.h"
//#include "Input.h"

#include <iostream>
#include <linux/input.h>

int main()
{
    //Dot::Input input;
    //Dot::Window * win;
    //win = new Dot::Window(40,40,0.1f);

    //while (true)
    {
        //win->BeginFrame();

        //if (input.KeyPressed())
        {
            //char k;
            //std::cin >> k;
        }

        //win->EndFrame();
    }    
}